# ShadowGrid Python dependencies — pinned to the known-good versions.
# Install into a venv:  python3 -m venv venv && ./venv/bin/pip install -r requirements.txt
#
# bleak is pinned deliberately: the BLE scan/connect code relies on the
# bleak 2.x API (BleakScanner.discover(..., return_adv=True)); a 3.x bump
# changes that surface and will break device discovery.
Flask==3.1.3
flask-cors==6.0.5
Werkzeug==3.1.8
bleak==2.0.0
dbus-fast==3.1.2
dbus-next==0.2.3
pycryptodome==3.23.0
ecdsa==0.18.0
pyserial==3.5
